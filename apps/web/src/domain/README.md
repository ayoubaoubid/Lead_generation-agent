# Domain layer

The domain layer contains deterministic entities, value objects, policies, state
transitions and business errors. It must remain independent from React, Next.js,
Supabase, Trigger.dev and provider SDKs.
