# Technical libraries

This directory contains small, technology-oriented primitives shared by multiple
layers, such as API envelopes and structured logging contracts. It must not become a
catch-all `utils` directory.
