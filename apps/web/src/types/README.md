# Cross-cutting types

Only stable types used by more than one layer belong here. Domain-specific types stay
inside their domain module, and database-generated types will remain isolated from
public API contracts.
